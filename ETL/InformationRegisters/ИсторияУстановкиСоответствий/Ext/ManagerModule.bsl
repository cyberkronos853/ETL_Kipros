 Функция ПолучитьТаблицуSQL(ОбъектИсточник) Экспорт
	
	Если ТипЗнч(ОбъектИсточник) = Тип("СправочникСсылка.ОбъектыБазКВыгрузке") Тогда
		возврат ОбъектИсточник.СтруктураТаблицы.Наименование; 
	ИначеЕсли ТипЗнч(ОбъектИсточник) = Тип("СправочникСсылка.ОбъектыКВыгрузке") Тогда
		возврат Стрзаменить(ОбъектИсточник.НаименованиеТаблицыSQL, "_tmp", ""); 
	Иначе
		возврат "";
	КонецЕсли;    
	
КонецФункции

