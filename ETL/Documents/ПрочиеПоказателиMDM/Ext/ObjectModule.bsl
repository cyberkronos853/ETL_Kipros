
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	Ответственный = Пользователи.ТекущийПользователь();
	ТекДата = ТекущаяДата();
	ГодОтчета = ТекДата;
	
	Статус = Перечисления.СтатусДокументаПрочиеПоказатели.Создан;
	ЗапрещеноРедактировать = Ложь;
	ПериодОтчета = Справочники.ПериодыПрочихПоказателей.Произвольный;
	КонецПериода = ТекДата;
	УстановитьНовыйНомер();
КонецПроцедуры
