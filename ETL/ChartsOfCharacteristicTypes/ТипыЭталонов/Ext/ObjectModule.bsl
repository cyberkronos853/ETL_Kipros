#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЭтоНовый() и Выгружать Тогда
		
		Если Не ОбщегоНазначения.КоллекцииИдентичны(ЭтотОбъект.Реквизиты, ЭтотОбъект.Ссылка.Реквизиты, "Реквизит, ИмяПоляSQL", ,Истина) Тогда
			
			МенеджерЗаписи = РегистрыСведений.ТаблицыЭталоновДляРеструктуризации.СоздатьМенеджерЗаписи();
			МенеджерЗаписи.ТипЭталона = ЭтотОбъект.Ссылка; 
			МенеджерЗаписи.Записать();  
			
		КонецЕсли
		
	КонецЕсли;

КонецПроцедуры

#КонецОбласти
