
&НаСервереБезКонтекста
Процедура ИнициализироватьТаблицыДляЭталоновНаСервере()
	ПланыВидовХарактеристик.ТипыЭталонов.ИнициализироватьТаблицыДляЭталонов();
КонецПроцедуры

&НаКлиенте
Процедура ИнициализироватьТаблицыДляЭталонов(Команда)
	ИнициализироватьТаблицыДляЭталоновНаСервере();
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИнициализироватьТаблицуМаппингаНаСервере()
	ПланыВидовХарактеристик.ТипыЭталонов.ИнициализироватьТаблицуСоответствий();
КонецПроцедуры

&НаКлиенте
Процедура ИнициализироватьТаблицуМаппинга(Команда)
	ИнициализироватьТаблицуМаппингаНаСервере();
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ПересоздатьТаблицуЭталоновПоУмолчаниюНаСервере()
	ПланыВидовХарактеристик.ТипыЭталонов.ИнициализироватьТаблицуЭталоновПоУмолчанию();
КонецПроцедуры

&НаКлиенте
Процедура ПересоздатьТаблицуЭталоновПоУмолчанию(Команда)
	ПересоздатьТаблицуЭталоновПоУмолчаниюНаСервере();
КонецПроцедуры
