 
 &НаСервереБезКонтекста
Процедура ВыполнитьСценарийНаСервере(Ссылка) 
	
	//Справочники.СценарииВыполненияПроцедурSQL.ВыполнитьСценарий(Ссылка);
	ОбменSQL = Обработки.ОбменSQL.Создать();
	ОбменSQL.ВыполнитьСценарий(Ссылка);
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьСценарий(Команда)
	
	Если Элементы.Список.ТекущаяСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВыполнитьСценарийНаСервере(Элементы.Список.ТекущаяСтрока);	
	
КонецПроцедуры
